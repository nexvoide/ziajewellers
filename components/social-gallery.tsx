import Image from 'next/image';import {Instagram} from 'lucide-react';import {socialImages} from '@/data/social';import {business} from '@/data/config';
export function SocialGallery(){return <section className="social section"><div className="gallery-copy reveal"><p className="eyebrow">THE ZIA GALLERY</p><h2>JEWELLERY,<br/><em>COMPOSED</em></h2><p>Discover gold through portrait, detail, and form—each frame considered around the piece it holds.</p>{business.instagram?<a className="btn ghost" href={business.instagram}><Instagram size={16}/> Follow on Instagram</a>:<span className="config-note">Instagram link pending configuration</span>}</div><div className="social-grid">{socialImages.map((item,index)=><div className={`reveal gallery-item item-${index+1}`} key={item.image}><Image src={item.image} alt={item.alt} fill sizes="(max-width: 900px) 50vw, 22vw"/></div>)}</div></section>}
