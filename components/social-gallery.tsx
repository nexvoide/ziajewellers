import Image from 'next/image';import {Instagram} from 'lucide-react';import {business} from '@/data/config';import type {WebsiteImage} from '@/lib/site-images';
export function SocialGallery({images}:{images:WebsiteImage[]}){return <section className="social section"><div className="gallery-copy reveal"><p className="eyebrow">THE ZIA GALLERY</p><h2>JEWELLERY,<br/><em>COMPOSED</em></h2><p>Discover gold through portrait, detail, and form—each frame considered around the piece it holds.</p>{business.instagram?<a className="btn ghost" href={business.instagram}><Instagram size={16}/> Follow on Instagram</a>:<span className="config-note">Instagram link pending configuration</span>}</div><div className="social-grid">{images.map((item,index)=><div className={`reveal gallery-item item-${index+1}`} key={item.slot}><Image src={item.imageUrl} alt={item.altText} fill sizes="(max-width: 900px) 50vw, 22vw" style={{objectPosition:item.objectPosition}}/></div>)}</div></section>}
