import Image from 'next/image';import {ArrowUpRight} from 'lucide-react';
import type {WebsiteImage} from '@/lib/site-images';
export function Heritage({image}:{image:WebsiteImage}){return <section id="story" className="story section"><div className="story-image reveal"><Image src={image.imageUrl} alt={image.altText} fill sizes="50vw" style={{objectPosition:image.objectPosition}}/></div><div className="story-copy reveal"><p className="eyebrow">OUR HERITAGE</p><h2>CRAFTED FOR<br/><em>GENERATIONS</em></h2><p>Every Zia piece carries more than craftsmanship. It carries moments, celebrations, promises, and stories that deserve to last.</p><a className="text-link" href="#visit">Our story <ArrowUpRight size={15}/></a></div></section>}
