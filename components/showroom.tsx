import {MapPin} from 'lucide-react';import {business} from '@/data/config';
export function Showroom(){const location=[business.address,business.city,business.country].filter(Boolean).join(', ');const mapEmbedUrl=`https://www.google.com/maps?q=${encodeURIComponent(business.googleMapsCoordinates)}&z=16&output=embed`;return <section id="visit" className="visit section"><div className="visit-copy reveal"><p className="eyebrow">THE ZIA EXPERIENCE</p><h2>VISIT ZIA<br/><em>JEWELLERS</em></h2><p>Discover our collections in person and let our specialists guide you toward a piece that feels entirely your own.</p><dl><div><dt>SHOWROOM</dt><dd>{location||'Location pending confirmation'}</dd></div><div><dt>HOURS</dt><dd>{business.openingHours||'Hours pending confirmation'}</dd></div><div><dt>COORDINATES</dt><dd>26°14&apos;43.9&quot;N, 68°24&apos;38.2&quot;E</dd></div><div><dt>APPOINTMENTS</dt><dd>{business.phone||business.email||'Contact details pending'}</dd></div></dl>{business.googleMapsUrl?<a className="btn gold" href={business.googleMapsUrl} target="_blank" rel="noopener noreferrer"><MapPin size={15}/> Get directions</a>:<span className="config-note">Directions will be available when the verified map location is configured.</span>}</div><div className="map-card map-embed reveal"><iframe src={mapEmbedUrl} title="Zia Jewellers location at Liaquat Market near Railway Station" loading="lazy" referrerPolicy="no-referrer-when-downgrade" allowFullScreen/><div className="map-label"><MapPin size={18}/><span>ZIA JEWELLERS</span><small>26.2455278, 68.4106111</small></div></div></section>}
